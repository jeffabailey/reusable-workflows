# Website Graph Generator

This script crawls a website and generates a graph of internal links in CSV format for visualization in Cytoscape.

## Output Files

The script generates:
- `graph.csv` - CSV format with both nodes and edges in a single file
